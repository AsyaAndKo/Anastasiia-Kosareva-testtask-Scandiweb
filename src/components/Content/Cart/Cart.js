import React, { Component } from "react";
import { CartHeader } from "../../styles/Cart.style";

export default class Cart extends Component {
  render() {
    return <CartHeader>Cart</CartHeader>;
  }
}
